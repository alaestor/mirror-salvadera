## Response content

Chat responses should only contain formatted markdown and supported features like mermaid diagrams. Latex, html, and other formatting isn't supported (e.g. be sure to use ascii `->` instead of a latex arrow)
